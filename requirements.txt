# Direct runtime requirements. The Docker image installs requirements.lock,
# which contains the fully resolved Python 3.12 closure. Regenerate that lock
# after changing this file and validate it with scripts/test-image.sh.
mcp==1.28.1
scrapli[asyncssh]==2025.1.30
scrapli-community==2025.1.30
ntc-templates==7.9.0
asyncpg==0.30.0
PyJWT==2.13.0
cryptography==50.0.0
httpx==0.28.1
pyyaml==6.0.2
